sudo: false

language: python
 
python:
    - "3.4"
    - "3.5"
    - "3.6"

install:
    - pip install pytest pytest-cov coveralls
 
script:
    - pytest --cov ngo

after_success:
  - coveralls